import React from "react"
import Layout from "../components/layout"

const projects = () => {
  return (
    <Layout>
      <div>
        <h1>Projects</h1>
        <p>this is where the projects will be listed dynamically</p>
      </div>
    </Layout>
  )
}

export default projects
